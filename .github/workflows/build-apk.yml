name: Build Android APK

on:
  push:
    branches: [ "master" ] # O kaya "main" depende sa branch mo

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies and build web app
        run: |
          cd pet-info
          npm install
          npm run build

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Build Android Debug APK
        run: |
          cd pet-info/android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v3
        with:
          name: app-debug
          path: pet-info/android/app/build/outputs/apk/debug/app-debug.apk